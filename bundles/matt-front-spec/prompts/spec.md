# Write the spec

We have finished the interview. Using everything I told you in this same
conversation, write the specification now.

Read the spec-writing skill you are pointed to and follow it. Publish the spec to
the tracker I chose: {{artifact:tracker}}. Use exactly that destination — do not
infer a different one from the repository.

- Local: write the spec as a single Markdown file at `specs/spec.md`, creating the
  `specs` directory if needed.
- GitHub: create the spec as one GitHub issue.
- Any other tracker: use the tool connected for it. If that tracker is not
  available to you, say so plainly instead of publishing somewhere else.

Keep it faithful to what we agreed — do not invent requirements we never discussed.
Publish only the spec. Do not run any other commands.
