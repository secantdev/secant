# Write the spec

We have finished the interview. Using everything I told you in this same
conversation, write the specification now.

Read the spec-writing skill you are pointed to and follow it. Write the spec as a
single Markdown file at `specs/spec.md`, creating the `specs` directory if needed.
Keep it faithful to what we agreed — do not invent requirements we never discussed.

Write only that one file. Do not run any other commands.
