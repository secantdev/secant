# Subprocess Test Migration Ledger

This ledger is the coverage contract for the evidence split authorised by the
[#177 resolution](https://github.com/secantdev/secant/issues/177#issuecomment-5755036178). It records every test file that reaches a real child at
`2d0142efc3f816820115a4dd4b9c6e3ab0b00ffb` before any test is rewritten or deleted. The issue #177 stress prototype remains branch-only evidence and
is deliberately excluded from this tree.

The target layers are **process-free semantic suite**, **standalone runtime conformance**, and **compiled-binary acceptance**. `open` means the old
subprocess-backed assertion still exists. Migrate rows independently: after a row's named replacement is green on Windows, macOS, and Linux, the
deletion change removes that old assertion and marks its row `done`. A whole file is deletable only when every one of its rows is `done`; rows remain
as historical coverage records after deletion.

| Test file                                                    | Assertion carried today                                                                                                        | Target layer                   | Replacement test                                                              | Status |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------ | ----------------------------------------------------------------------------- | ------ |
| `tests/application/answer-human-gate.test.ts`                | Human Gate answers preserve ordering, ownership, artifacts, and resting state.                                                 | process-free semantic suite    | Same suite over injected Process and Harness doubles.                         | open   |
| `tests/application/cancel-delete-run.test.ts`                | Cancel and delete Operations settle against the correct live or resting Run.                                                   | process-free semantic suite    | Same suite over injected Process and Harness doubles.                         | open   |
| `tests/application/codex-shared-client-interactions.test.ts` | Codex Turn events and client interactions project through the shared Port.                                                     | process-free semantic suite    | Same client-interaction cases over the Harness and Process fakes.             | open   |
| `tests/application/harness-identity.test.ts`                 | Selected and observed Harness identity survive launch, failure, and resume.                                                    | process-free semantic suite    | Same identity cases over literal fake Harness registrations and fake Process. | open   |
| `tests/application/harness-request-overlay.test.ts`          | Live Harness requests, answers, expiry, and indeterminate receipts project correctly.                                          | process-free semantic suite    | Same overlay cases over the Harness and Process fakes.                        | open   |
| `tests/application/interrupt-resume-turn.test.ts`            | Interrupt, steer refusal, detach, resume, and unacknowledged recovery map to durable Run state.                                | process-free semantic suite    | Same lifecycle cases over the Harness and Process fakes.                      | open   |
| `tests/application/legacy-run-harness-upgrade.test.ts`       | Legacy Runs acquire the durable Harness identity without losing recovery behavior.                                             | process-free semantic suite    | Same upgrade cases over the Harness and Process fakes.                        | open   |
| `tests/application/materialization.test.ts`                  | Materialization conflict and recovery outcomes project through Operations.                                                     | process-free semantic suite    | Same materialization cases over injected Process.                             | open   |
| `tests/application/operation-settlement.test.ts`             | Operations settle once with their admitted, applied, or indeterminate result.                                                  | process-free semantic suite    | Same settlement cases over injected Process and Harness doubles.              | open   |
| `tests/application/preflight.test.ts`                        | Preflight refuses invalid Workspace, executable, platform, and Harness facts before Run creation.                              | process-free semantic suite    | Same Preflight matrix over injected Process resolution and command results.   | done   |
| `tests/application/preflight.test.ts`                        | Classic, unborn, linked, bare, nested, and non-repository Git Workspaces are distinguished by the real probe.                  | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `preflight-git-worktree`.            | done   |
| `tests/application/run-list.test.ts`                         | Run-list projections retain ordering, filters, paging, and live markers.                                                       | process-free semantic suite    | Same list cases over injected Process and Harness doubles.                    | open   |
| `tests/application/run-projection.test.ts`                   | Run projections preserve durable/live joins, actions, history, and resting evidence.                                           | process-free semantic suite    | Same projection cases over injected Process and Harness doubles.              | open   |
| `tests/application/transcript-resource.test.ts`              | Transcript resources preserve paging, bounds, and access semantics.                                                            | process-free semantic suite    | Same resource cases over injected Process and Harness doubles.                | open   |
| `tests/headless/agent-step.test.ts`                          | Agent launch, Harness selection, mixed routing, and observed identity render correctly.                                        | process-free semantic suite    | Same headless cases through the double-backed headless harness.               | open   |
| `tests/headless/codex-client-interactions.test.ts`           | Codex approvals and frozen JSON output agree with the shared client contract.                                                  | process-free semantic suite    | Same CLI cases over the Codex Harness fake and fake Process.                  | open   |
| `tests/headless/harness-request-policy.test.ts`              | Harness-request policy prepares, answers, and closes exactly the selected Harness.                                             | process-free semantic suite    | Same policy cases over literal fake Harness registrations.                    | open   |
| `tests/headless/harness-request.test.ts`                     | Allow, deny, and live-overlay request behavior renders through headless commands.                                              | process-free semantic suite    | Same request cases over the Harness and Process fakes.                        | open   |
| `tests/headless/headless.test.ts`                            | Workspace and Bundle commands keep their text, JSON, validation, and exit-code contracts.                                      | process-free semantic suite    | Same commands through the double-backed headless harness.                     | open   |
| `tests/headless/interactive-agent.test.ts`                   | Headless rejects Interactive agent steps without changing Run truth.                                                           | process-free semantic suite    | Same refusal through the double-backed headless harness.                      | open   |
| `tests/headless/materialization.test.ts`                     | Materialization conflicts and resume are reported through stable headless output.                                              | process-free semantic suite    | Same CLI cases over fake Process command scripts.                             | open   |
| `tests/headless/resume.test.ts`                              | Resume, ownership refusal, takeover, and exit codes preserve the Run contract.                                                 | process-free semantic suite    | Same CLI cases through the double-backed headless harness.                    | open   |
| `tests/headless/resume.test.ts`                              | A child owner can die and a later invocation can recover its Run.                                                              | compiled-binary acceptance     | `scripts/package-smoke.ts` — `owner-death-recovery`.                          | open   |
| `tests/headless/run-list-cancel-delete.test.ts`              | List, cancel, and delete commands expose the correct offers and results.                                                       | process-free semantic suite    | Same CLI cases through the double-backed headless harness.                    | open   |
| `tests/headless/run.test.ts`                                 | Command routing, Repeat groups, Human Gates, artifacts, and frozen output shapes hold end to end.                              | process-free semantic suite    | Same CLI cases over fake Process command scripts.                             | open   |
| `tests/headless/test-repair-e2e.test.ts`                     | The Proof Bundle reaches its authored gate and succeeds for both Harness selections.                                           | process-free semantic suite    | Same routing assertions over fake Harnesses and fake Process.                 | open   |
| `tests/run/store/artifacts/artifacts.test.ts`                | Artifact publication, lookup, identity, and Git-unavailable failures obey the private repository contract.                     | process-free semantic suite    | Same repository contract over injected Process results.                       | done   |
| `tests/run/store/artifacts/artifacts.test.ts`                | Real Git object creation and reading agree with the Process-backed repository contract.                                        | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `artifact-git-repository`.           | done   |
| `tests/run/store/attempt-and-artifact-publication.test.ts`   | Attempt and Artifact publication commit atomically and recover without partial truth.                                          | process-free semantic suite    | Same store cases over injected Process.                                       | done   |
| `tests/run/store/concurrent-writer.test.ts`                  | A concurrent writer cannot bypass ownership or fencing.                                                                        | process-free semantic suite    | Same coordination cases over deterministic competing owners.                  | done   |
| `tests/run/store/concurrent-writer.test.ts`                  | Separate processes contend against the real coordination store.                                                                | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `store-concurrent-writer`.           | done   |
| `tests/run/store/materialization.test.ts`                    | Materialized files publish, detect conflicts, and recover with exact bytes.                                                    | process-free semantic suite    | Same store cases over injected Process.                                       | done   |
| `tests/run/store/ownership-and-recovery.test.ts`             | Claims, fencing, takeover, and recovery preserve durable ownership rules.                                                      | process-free semantic suite    | Same ownership state machine over deterministic owners.                       | done   |
| `tests/run/store/ownership-and-recovery.test.ts`             | A locked coordination database is never mistaken for corruption.                                                               | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `store-locked-coordination`.         | done   |
| `tests/run/store/reconcile-turn.test.ts`                     | Turn reconciliation records each terminal kind once and preserves Session evidence.                                            | process-free semantic suite    | Same reconciliation cases over injected Harness and Process doubles.          | done   |
| `tests/run/store/session-and-transcript-evidence.test.ts`    | Session availability and transcript evidence remain durable and bounded.                                                       | process-free semantic suite    | Same evidence cases over injected Harness and Process doubles.                | done   |
| `tests/run/execution/agent.test.ts`                          | Every Turn result maps to the correct Attempt, Session availability, and resting state.                                        | process-free semantic suite    | Same execution-domain suite over the Harness and Process fakes.               | done   |
| `tests/run/execution/execution.test.ts`                      | Command outcomes, retries, Repeat groups, aborts, assets, and resting states obey routing.                                     | process-free semantic suite    | Same routing suite over scripted fake Process results.                        | done   |
| `tests/run/execution/execution.test.ts`                      | Real command output, exit, cancellation, group reaping, and Git commit behavior match execution's contract.                    | standalone runtime conformance | `tests/process/runtime-conformance.ts` — the `execution-real-*` scenarios.    | done   |
| `tests/run/execution/materialization.test.ts`                | Produced Workspace files, tamper conflicts, exact bytes, and resume obey materialization rules.                                | process-free semantic suite    | Same materialization suite over scripted fake Process results.                | done   |
| `tests/process/spawn.test.ts`                                | Real child env, output draining, tree reaping, graceful interruption, and escalation obey the Process contract.                | standalone runtime conformance | Process parity plus `process-worker-environment` in the runtime runner.       | done   |
| `tests/harness/claude-code.test.ts`                          | Recorded Claude Code prepare, Turn, request, interruption, resume, and failure traffic satisfies Harness conformance.          | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `claude-code-replayer-conformance`.  | open   |
| `tests/harness/codex.test.ts`                                | Recorded Codex discovery, app-server traffic, requests, steer, interruption, resume, and failures satisfy Harness conformance. | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `codex-replayer-conformance`.        | open   |
| `tests/tui/matt-front-workbench.test.tsx`                    | The recorded Matt front traverses the Workbench, two Human Gates, recovery, and completion.                                    | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `matt-front-replayer-workbench`.     | open   |
| `tests/composition/wiring.test.ts`                           | The composition root supplies one coherent Catalog, Run Store, execution, assets, and client Port.                             | process-free semantic suite    | Same wiring suite with an explicitly injected fake Process.                   | done   |
| `tests/composition/wiring.test.ts`                           | The shipped composition executes Command, Harness, recovery, and Git through the real adapters.                                | compiled-binary acceptance     | Existing named paths in `scripts/package-smoke.ts`.                           | done   |
| `tests/architecture/migrations.test.ts`                      | The real migration generator rejects schema drift and names regeneration.                                                      | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `migration-generator-drift`.         | open   |
| `tests/architecture/vendor-provenance.test.ts`               | Real declaration emission keeps fenced packages out of Module entry surfaces.                                                  | standalone runtime conformance | `tests/process/runtime-conformance.ts` — `entry-declaration-surface`.         | open   |
| `tests/release/posix-installer.test.ts`                      | The POSIX installer verifies, installs, replaces, refuses malformed candidates, and updates PATH.                              | compiled-binary acceptance     | Existing `POSIX installer consumer` step in the three-OS consumer job.        | open   |
